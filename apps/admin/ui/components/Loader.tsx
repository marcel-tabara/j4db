import { LinearProgress } from '@mui/material';

const Loader = () => {
  return (
    <div className="genericContainer">
      <LinearProgress />
    </div>
  );
};

export default Loader;
